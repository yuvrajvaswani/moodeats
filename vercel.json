{
  "framework": "vite",
  "installCommand": "if [ -d frontend ]; then npm --prefix frontend install; else npm install; fi",
  "buildCommand": "if [ -d frontend ]; then npm --prefix frontend run build; else npm run build; mkdir -p frontend; rm -rf frontend/dist; cp -r dist frontend/dist; fi",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
